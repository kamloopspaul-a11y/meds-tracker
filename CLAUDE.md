# CLAUDE.md

> ⚠️ **Studio root must be mounted before starting work.**
>
> If `~/Documents/Studio` is not already mounted in this session, call `request_cowork_directory` with that path now. Root contains `CLAUDE.md` (session protocol), `TODO_LIST.md` (cross-project cadence cues), and `.claude-config` (API keys and credentials used across all projects). Without root you are missing cadence checks, credentials, and cross-project context.
>
> Once root is confirmed mounted, read the relevant `Projects/<folder>/PROJECT.md` to restore session context for this project.
